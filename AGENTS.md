# Project Notes

- Run the prototype through a local static server so `fetch()` can load JSON content: `python -m http.server 8000` from the repository root.
- Quick checks: `node --check js/game.js` and parse all `world/**/*.json`.
